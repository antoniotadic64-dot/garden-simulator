{
  "name": "Python Codespace",

  "image": "mcr.microsoft.com/devcontainers/python:3.12",

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ],
      "settings": {
        "workbench.colorTheme": "Dark Modern"
      }
    }
  },
  
  "postCreateCommand": "code --uninstall-extension github.copilot-chat --force || true; code --uninstall-extension github.copilot --force || true",
  "postAttachCommand": "code --uninstall-extension github.copilot-chat --force || true; code --uninstall-extension github.copilot --force || true"
}